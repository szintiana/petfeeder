import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'live-streaming-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
